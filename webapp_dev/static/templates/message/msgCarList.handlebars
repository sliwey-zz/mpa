{{#each carList}}
<li class="message-car-item">
    <p class="message-sub-title" data-number="{{getLen list}}">
        <label class="message-sub-comp"><input class="cb-comp" type="checkbox">{{corp}}</label>
    </p>
    <ul class="message-sub-list">
        {{#each list}}
        <li class="message-sub-item"><label><input class="cb-car" type="checkbox" data-no="{{getNo vehicleNumber}}">浙BT{{getNo vehicleNumber}}</label></li>
        {{/each}}
    </ul>
</li>
{{/each}}